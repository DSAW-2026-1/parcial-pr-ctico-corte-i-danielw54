promt 1: Genera la estructura HTML semántica para un componente de interfaz. El contenedor principal debe estar dentro de <main> y contener una <section> de 400px x 300px. No utilices <div>. Usa listas (<ul> y <li>) para agrupar las figuras por esquina. Debe haber cuatro grupos: Superior izquierda: cuadrado seguido de círculo. Superior derecha: círculo seguido de cuadrado. Inferior izquierda: círculo seguido de cuadrado. Inferior derecha: cuadrado seguido de círculo. Usa clases reutilizables (.square y .circle).

promt 2: Genera una hoja de estilos externa (style.css). Aplica un reset usando el selector universal y define box-sizing: border-box. El contenedor debe medir exactamente 400px x 300px. Céntralo vertical y horizontalmente usando height: 100vh. Define los siguientes estilos: Fondo azul oscuro para el contenedor. Cuadrados color cyan claro. Círculos color rosado con border-radius: 50%. Las figuras deben medir 55px x 55px. Mantén la especificidad baja usando clases.

prompt 3: Utiliza CSS Grid en el contenedor principal para dividirlo en una matriz 2x2. Configura: display: grid;grid-template-columns: 1fr 1fr; grid-template-rows: 1fr 1fr; Posiciona cada grupo usando justify-self y align-self. Dentro de cada grupo usa Flexbox para alinear las figuras horizontalmente y usa gap para el espaciado. Céntralo completamente en el viewport usando Flexbox en el elemento padre.